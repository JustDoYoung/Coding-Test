#include <bits/stdc++.h>

using namespace std;

int main()
{
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);

    size_t input;
    cin >> input;

    cout << input * (input - 1) / 2 << '\n';
    cout << 2 << '\n';

    return 0;
}