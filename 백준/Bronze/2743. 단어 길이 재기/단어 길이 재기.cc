#include <iostream>
#include <cstring>
using namespace std;

int main() {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	char input[101];
	cin >> input;

	cout << strlen(input) << endl;

	return 0;
}