#include <iostream>
using namespace std;

int main() {

	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	char input[101];
	while (cin.getline(input, 101)) {
		cout << input << endl;
	}

	return 0;
}